# Unpacked packages

No ASAR, ZIP, UXP package, CEP package, or other losslessly extractable MCP implementation package was found among the identified MCP components. Both candidates are native `.aip` plug-in bundles and were copied intact under `../copied-files/`.
